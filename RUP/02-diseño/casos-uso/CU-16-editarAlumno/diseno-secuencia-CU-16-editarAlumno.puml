@startuml secuencia-cu16-editar-alumno
title Diagrama de Secuencia - Editar Alumno

actor "Docente" as Actor
participant "Frontend\n(React)" as FE
participant "ControladorAlumno" as Controller
participant "ServicioAlumno" as Service
participant "RepositorioGrado" as RepoGrado
participant "RepositorioAlumno" as RepoAlumno
database "Base de Datos\n(PostgreSQL)" as DB

Actor -> FE : Modifica datos del alumno
activate FE

FE -> Controller : POST /api/alumnos (DTO_Alumno con ID)
activate Controller

Controller -> Service : guardarIndividual(dto)
activate Service

Service -> RepoGrado : findByCodigo(codigoGrado)
activate RepoGrado
RepoGrado -> DB : SELECT grado
DB --> RepoGrado : resultado
RepoGrado --> Service : Optional<Grado>
deactivate RepoGrado

Service -> RepoAlumno : findById(id)
activate RepoAlumno
RepoAlumno -> DB : SELECT alumno
DB --> RepoAlumno : resultado
RepoAlumno --> Service : Optional<Alumno>
deactivate RepoAlumno

Service -> RepoAlumno : save(alumnoActualizado)
activate RepoAlumno
RepoAlumno -> DB : UPDATE alumno
DB --> RepoAlumno : ok
RepoAlumno --> Service : alumno
deactivate RepoAlumno

Service --> Controller : void
deactivate Service

Controller --> FE : 200 OK
deactivate Controller

FE --> Actor : Muestra alumno actualizado
deactivate FE

@enduml
