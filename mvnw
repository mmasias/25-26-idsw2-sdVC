#!/bin/sh
MAVEN_HOME="$HOME/.m2/wrapper/dists/apache-maven-3.9.6"
if [ ! -d "$MAVEN_HOME" ]; then
  echo "Downloading Maven..."
  mkdir -p "$HOME/.m2/wrapper/dists"
  curl -fsSL "https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.6/apache-maven-3.9.6-bin.zip" -o /tmp/maven-dist.zip
  unzip -qo /tmp/maven-dist.zip -d "$HOME/.m2/wrapper/dists"
fi
export M2_HOME="$MAVEN_HOME"
"$M2_HOME/bin/mvn" "$@"
